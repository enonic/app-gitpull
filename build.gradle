plugins {
    id 'jacoco'
    id 'com.enonic.defaults' version '2.0.1'
    id 'com.enonic.xp.app' version '3.1.0'
    id 'maven-publish'
}

repositories {
    mavenCentral()
    xp.enonicRepo()
}

app {
    name = 'com.enonic.app.gitpull'
    displayName = 'GitPull App'
    vendorName = 'Enonic AS'
    vendorUrl = 'http://enonic.com'
    systemVersion = "${xpVersion}"
}

dependencies {
    compile "com.enonic.xp:core-api:${xpVersion}"
    compile "com.enonic.xp:jaxrs-api:${xpVersion}"
    include 'org.eclipse.jgit:org.eclipse.jgit:6.0.0.202111291000-r'
    include 'org.eclipse.jgit:org.eclipse.jgit.ssh.jsch:5.13.0.202109080827-r'
    testCompile 'junit:junit:4.13.2'
    testCompile 'org.mockito:mockito-core:+'
    testCompile "com.enonic.xp:testing:${xpVersion}"
}
